{
	"name": "gatekeeperhq",
	"version": "0.1.0",
	"private": true,
	"description": "RBAC Admin Panel with .NET backend and Next.js frontend",
	"scripts": {
		"prepare": "husky",
		"format:server": "cd server && dotnet format GatekeeperHQ.sln",
		"format:client": "cd client && npx prettier --write .",
		"format": "npm run format:server && npm run format:client"
	},
	"devDependencies": {
		"husky": "^9.1.7"
	}
}